[package]
name = "rss-reporter-core"
version = "0.1.0"
edition = "2024"

[dependencies]
feed-rs = "2.3.1"

serde = { version = "1", features = ["derive"] }
config = "0.15"
reqwest = { version = "0.13.2", features = ["blocking"]}

percent-encoding = "2"